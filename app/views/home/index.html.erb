<!-- Carousel
================================================== -->
<div id="myCarousel" class="carousel slide">
  <div class="carousel-inner">
    <% Photo.all.each_with_index do |photo, index| %>
    <div class="item">
      <%= image_tag photo.attachment.media %>
      <div class="container">
        <div class="carousel-caption">
          <h1><%= photo.title %></h1>
          <p class="lead"><%= raw photo.note %></p>
          <% if @product %>
            <%= link_to content_tag(:li, "", :class=>'icon-download') + " 立即下载", download_product_path(@product), class: "btn btn-large btn-primary" %>
            <p>运行环境：Windows XP或更高版本(32位)</p>
            <p>版本：<%= @product.release_version %></p>
            <p>发布日期：<%= @product.release_date %></p>
          <% end %>
        </div>
      </div>
    </div>
    <% end %>
  </div>
  <a class="left carousel-control" href="#myCarousel" data-slide="prev">&lsaquo;</a>
  <a class="right carousel-control" href="#myCarousel" data-slide="next">&rsaquo;</a>
</div><!-- /.carousel -->

<div class="container video">

  <!-- Three columns of video below the carousel -->
  <% (0..@row_count-1).each do |row_index| %>
  <div class="row-fluid">
    <% (0..2).each do |col_index| %>
      <% video = @videos[row_index * 3 + col_index] %>
      <% next if video.nil? %>
      <div class="span4">
        <%= link_to(image_tag(video.photo_attachment.media.preview), video_path(video), target: "_blank") if video.photo_attachment %>
        <h4 class="muted"><%= truncate video.title, length: 20 %></h4>
        <p><%= raw truncate(video.note, length: 80) %></p>
        <%= link_to(
            " 播放视频 ▶", 
              video_path(video, autoplay: true), 
              class: "btn btn-primary", target: "_blank") %>
      </div>
    <% end %>
  </div>
  <hr>
  <% end %>

  <!-- START THE FEATURETTES -->
  <div class="featurette row-fluid">
    <div class="span6">
      <h2 class="featurette-heading">大唐L1测试手机 <span class="muted">高通芯片解决方案</span></h2>
      <p class="lead">业务唯一高通芯片系统化路测解决方案，业内首款LTE专业路测手机，实现了商用终端和测试终端的同机一体，更真实反馈用户感知。<br>支持GSM/WCDMA（联通）/TD-SCDMA(移动3G)/TDD-LTE/FDD-LTE目前的所有网络制式。<br>专业厂家定制测试手机，采用Android系统，具备强制测试所需的所有功能（ 如强制锁频、强制锁小区、强制切换等）和深度定制能力</p>
    </div>
    <div class="span6">
      <%= image_tag("L1.jpg", class: "featurette-image pull-right") %>
    </div>
  </div>

  <hr class="featurette-divider">
  <div class="featurette row-fluid">
    <div class="span6">
      <%= image_tag("snapdragon.jpg", class: "featurette-image pull-left") %>
    </div>
    <div class="span6">
      <h2 class="featurette-heading">高通骁龙处理器 <span class="muted">70%芯片份额</span></h2>
      <p class="lead">骁龙S4 Plus MSM8930，该芯片的主要特色是同时支持UMTS、CDMA和TD-SCDMA制式，从而使得完美兼容三网。<br>中国联通、中国电信与中国移动都可以完美共存使用。而且值得一提的是，该单一平台处理器同时支持LTE-TDD和TD-SCDMA，主要面向在中国使用的中端智能手机。</p>
    </div>
  </div>

  <hr class="featurette-divider">

  <div class="featurette row-fluid">
    <div class="span6">
      <h2 class="featurette-heading">SPAN Outum V8 <span class="muted">实时路测</span></h2>
      <p class="lead">稳定性高：软件版本稳定性高,操作简便。<br>测试计划：支持灵活定制脚本序列。<br>支持对于测试数据的实时统计。<br>提供强大的数据展现：数据显示界面可定制。</p>
    </div>
    <div class="span6">
      <%= image_tag("outum_main.png", class: "featurette-image pull-right") %>
    </div>
  </div>

  <hr class="featurette-divider">

  <div class="featurette row-fluid">
    <div class="span6">
      <%= image_tag("outum_map.png", class: "featurette-image pull-left") %>
    </div>
    <div class="span6">
      <h2 class="featurette-heading">SPAN Outum V8 <span class="muted">后台分析</span></h2>
      <p class="lead">提供灵活的数据统计方式和报表生成功能
。<br>提供灵活的通用分析手段和专业化的专题分析</p>
    </div>
  </div>

  <hr class="featurette-divider">
  
  <!-- /END THE FEATURETTES -->
  <p id="#back-to-top"><a href="#">回到顶部</a></p>

</div><!-- /.container -->



